*.js
!jest.config.js
*.d.ts
node_modules
.pnpm-store
.claude

# CDK asset staging directory
.cdk.staging
cdk.out

# --- env (dotenvx) ---
# Encrypted .env.<environment> files ARE committed (values are ciphertext).
# NEVER commit the private keys or local-only overrides:
.env.keys
.env
.env.local
.env.*.local
