*.js
!jest.config.js
*.d.ts
node_modules
.pnpm-store
.claude

# CDK asset staging directory
.cdk.staging
cdk.out

# --- env ---
# Real env files are LOCAL ONLY and must never be committed.
# Only .env.example (the committed template) is allowed through.
.env
.env.*
!.env.example
